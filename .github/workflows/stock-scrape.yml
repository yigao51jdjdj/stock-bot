name: Stock Data Scraper

on:
  # 定时运行 - 每日三次
  schedule:
    # 早上 9:00 (北京时间)
    - cron: '0 1 * * 1-5'
    # 中午 13:00 (北京时间)
    - cron: '0 5 * * 1-5'
    # 晚上 22:00 (北京时间)
    - cron: '0 14 * * 1-5'

  # 手动触发
  workflow_dispatch:
    inputs:
      symbols:
        description: '股票代码 (逗号分隔)'
        required: false
        default: ''
      output_format:
        description: '输出格式 (json/csv)'
        required: false
        default: 'json'
      notify_type:
        description: '通知类型 (price/summary)'
        required: false
        default: 'summary'

env:
  PYTHON_VERSION: '3.9'

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create config file
        run: |
          cat > config_actions.json << EOF
          {
            "global_symbols": ["MSTR", "CRCL", "AXTI", "TSM", "NBIS", "PLTR", "RKLB", "SNDK", "HOOD", "COIN", "ASTS", "MU"],
            "a_share_symbols": [],
            "data_dir": "data",
            "output_format": "json",
            "notifications": {
              "email": {
                "enabled": true,
                "smtp_host": "smtp.mail.me.com",
                "smtp_port": 465,
                "use_ssl": true,
                "sender": "$EMAIL_SENDER",
                "password": "$EMAIL_PASSWORD",
                "receiver": "$EMAIL_RECEIVER"
              }
            }
          }
          EOF
        env:
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVER: ${{ secrets.EMAIL_RECEIVER }}

      - name: Run stock data scraper
        env:
          STOCK_SYMBOLS: ${{ github.event.inputs.symbols }}
          OUTPUT_FORMAT: ${{ github.event.inputs.output_format }}
          NOTIFY_TYPE: ${{ github.event.inputs.notify_type }}
        run: |
          python src/main.py --config config_actions.json --output ${OUTPUT_FORMAT:-json} --notify-type ${NOTIFY_TYPE:-summary}

      - name: Upload data artifacts
        uses: actions/upload-artifact@v4
        with:
          name: stock-data-${{ github.run_id }}
          path: data/
          retention-days: 30

      - name: Commit and push data (optional)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/
          git diff --staged --quiet || git commit -m "Update stock data: $(date +'%Y-%m-%d %H:%M')"
          git push || echo "No changes to push"